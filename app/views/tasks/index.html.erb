
<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
  <li>
    <input type="checkbox" <%= task.completed ? "checked": "unchecked" %>>
    <h1><%= link_to task.title, task_path(task.id) %></h1>
  </li>
  <% end %>
</ul>

<p><%= link_to 'add a new task', tasks_new_path %></p>
